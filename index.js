const Saga = require("./lib/component.saga").default
const Store = require("./lib/component.store").default
const HomePage = require("./lib/pages/home.index").default

module.exports = {
    Saga , Store , HomePage
}